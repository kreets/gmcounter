import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hu.devpartner.gmcounter',
  appName: 'GM Score-counter',
  webDir: 'dist'
};

export default config;
